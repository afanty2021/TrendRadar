# 本地运行部署

<cite>
**本文档引用文件**  
- [setup-mac.sh](file://setup-mac.sh)
- [setup-windows.bat](file://setup-windows.bat)
- [setup-windows-en.bat](file://setup-windows-en.bat)
- [start-http.sh](file://start-http.sh)
- [start-http.bat](file://start-http.bat)
- [requirements.txt](file://requirements.txt)
- [config/config.yaml](file://config/config.yaml)
- [pyproject.toml](file://pyproject.toml)
- [README-Cherry-Studio.md](file://README-Cherry-Studio.md)
</cite>

## 目录
1. [简介](#简介)
2. [前置条件](#前置条件)
3. [macOS 部署指南](#macos-部署指南)
4. [Windows 部署指南](#windows-部署指南)
5. [启动 HTTP 服务](#启动-http-服务)
6. [常见问题与解决方案](#常见问题与解决方案)
7. [附录](#附录)

## 简介

本指南详细说明如何在本地运行 TrendRadar MCP 项目。涵盖 macOS 和 Windows 操作系统的具体部署步骤，包括 Python 环境准备、依赖安装、配置文件初始化以及 HTTP 服务的启动与验证。同时提供命令行输出示例和预期结果，并列出本地运行时可能遇到的问题及解决策略。

## 前置条件

在开始部署之前，请确保满足以下前置条件：

- **操作系统**：macOS 或 Windows
- **Python 版本**：3.10 或更高版本
- **项目代码**：已从 GitHub 克隆或下载项目代码
- **网络连接**：稳定的互联网连接，用于下载依赖包

**Section sources**
- [pyproject.toml](file://pyproject.toml#L5)
- [README-Cherry-Studio.md](file://README-Cherry-Studio.md#L25-L28)

## macOS 部署指南

对于 macOS 用户，可以通过 `setup-mac.sh` 脚本自动完成 Python 依赖的安装和执行权限的设置。

### 步骤 1：打开终端

1. 打开“启动台”并搜索“终端”。
2. 打开终端应用程序。

### 步骤 2：运行部署脚本

1. 将项目文件夹中的 `setup-mac.sh` 文件拖拽到终端窗口。
2. 按回车键执行脚本。

```bash
./setup-mac.sh
```

### 步骤 3：检查输出

脚本将自动执行以下操作：
1. 检查并安装 UV 包管理器。
2. 使用 UV 安装项目依赖。
3. 检查配置文件 `config/config.yaml` 是否存在。
4. 为 `start-http.sh` 添加执行权限。

**预期输出示例：**
```
╔════════════════════════════════════════╗
║  TrendRadar MCP 一键部署 (Mac)        ║
╚════════════════════════════════════════╝

📍 项目目录: /Users/username/Github/TrendRadar

[1/3] ✅ UV 已安装
uv 0.2.0 (x86_64-apple-darwin)

[2/3] 📦 安装项目依赖...
提示: 这可能需要 1-2 分钟，请耐心等待

[3/3] ✅ 检查配置文件...

╔════════════════════════════════════════╗
║           部署完成！                   ║
╚════════════════════════════════════════╝

📋 下一步操作:

  1️⃣  打开 Cherry Studio
  2️⃣  进入 设置 > MCP Servers > 添加服务器
  3️⃣  填入以下配置:

      名称: TrendRadar
      描述: 新闻热点聚合工具
      类型: STDIO
      命令: /Users/username/.cargo/bin/uv
      参数（每个占一行）:
        --directory
        /Users/username/Github/TrendRadar
        run
        python
        -m
        mcp_server.server
```

**Section sources**
- [setup-mac.sh](file://setup-mac.sh#L1-L119)

## Windows 部署指南

对于 Windows 用户，可以使用 `setup-windows.bat` 或 `setup-windows-en.bat` 脚本进行部署。这两个脚本的主要区别在于语言支持和编码设置。

### `setup-windows.bat` 与 `setup-windows-en.bat` 的区别

- **`setup-windows.bat`**：支持中文输出，使用 UTF-8 编码（`chcp 65001`），适合中文用户。
- **`setup-windows-en.bat`**：英文输出，适合英文用户或在英文系统环境下使用。

### 步骤 1：运行部署脚本

1. 双击运行 `setup-windows.bat` 或 `setup-windows-en.bat`。
2. 如果遇到权限问题，右键选择“以管理员身份运行”。

### 步骤 2：检查输出

脚本将自动执行以下操作：
1. 验证项目结构。
2. 检查 Python 是否已安装。
3. 检查并安装 UV 包管理器。
4. 使用 UV 安装项目依赖。
5. 检查配置文件 `config/config.yaml` 是否存在。

**预期输出示例：**
```
==========================================
  TrendRadar MCP 一键部署 (Windows)
==========================================

📍 项目目录: C:\Users\username\Github\TrendRadar

[0/4] 🔍 验证项目结构...
✅ pyproject.toml 已找到

[1/4] 🐍 检查 Python...
✅ Python 3.10.12

[2/4] 🔧 检查 UV...
✅ uv 0.2.0 (x86_64-pc-windows-msvc)

[3/4] 📦 安装项目依赖...
工作目录: C:\Users\username\Github\TrendRadar
✅ 依赖安装成功

[4/4] ⚙️  检查配置文件...
✅ config\config.yaml 已存在

==========================================
            部署完成！
==========================================

📋 MCP 服务器配置信息（用于 Claude Desktop）:

  命令: C:\Users\username\AppData\Local\Programs\Python\Python310\Scripts\uv.exe
  工作目录: C:\Users\username\Github\TrendRadar

  参数（逐行填入）:
    --directory
    C:\Users\username\Github\TrendRadar
    run
    python
    -m
    mcp_server.server

📖 详细教程: README-Cherry-Studio.md

请按任意键继续...
```

**Section sources**
- [setup-windows.bat](file://setup-windows.bat#L1-L181)
- [setup-windows-en.bat](file://setup-windows-en.bat#L1-L176)

## 启动 HTTP 服务

部署完成后，可以通过 `start-http.sh`（Linux/macOS）或 `start-http.bat`（Windows）启动 HTTP 服务。

### 步骤 1：启动服务

- **macOS/Linux**：
  ```bash
  ./start-http.sh
  ```

- **Windows**：
  双击运行 `start-http.bat`。

### 步骤 2：验证服务

服务启动后，将在终端显示以下信息：
```
╔════════════════════════════════════════╗
║  TrendRadar MCP Server (HTTP 模式)    ║
╚════════════════════════════════════════╝

[模式] HTTP (适合远程访问)
[地址] http://localhost:3333/mcp
[提示] 按 Ctrl+C 停止服务
```

### 步骤 3：访问服务

打开浏览器，访问 `http://localhost:3333/mcp`，确认服务正常运行。

**Section sources**
- [start-http.sh](file://start-http.sh#L1-L22)
- [start-http.bat](file://start-http.bat#L1-L26)

## 常见问题与解决方案

### 1. Python 版本不兼容

**问题**：脚本提示 Python 版本过低。

**解决方案**：
- 访问 [Python 官网](https://www.python.org/downloads/) 下载并安装 Python 3.10 或更高版本。
- 确保 Python 已添加到系统 PATH 环境变量中。

### 2. 依赖安装失败

**问题**：UV 安装依赖时失败。

**解决方案**：
- 检查网络连接是否正常。
- 尝试使用 `uv sync --verbose` 获取详细错误信息。
- 手动安装依赖：`pip install -r requirements.txt`。

### 3. 端口占用

**问题**：启动 HTTP 服务时提示端口 3333 已被占用。

**解决方案**：
- 使用 `netstat -an | grep 3333`（macOS/Linux）或 `netstat -ano | findstr :3333`（Windows）查找占用端口的进程。
- 终止占用端口的进程或修改服务端口。

### 4. 配置文件缺失

**问题**：脚本提示 `config/config.yaml` 文件不存在。

**解决方案**：
- 检查项目根目录下是否存在 `config` 文件夹。
- 从 `config/config.example.yaml` 复制并重命名为 `config.yaml`。
- 编辑 `config.yaml` 文件，填入必要的 API 密钥和其他配置。

**Section sources**
- [setup-mac.sh](file://setup-mac.sh#L79-L84)
- [setup-windows.bat](file://setup-windows.bat#L139-L147)
- [setup-windows-en.bat](file://setup-windows-en.bat#L135-L142)

## 附录

### 配置文件说明

`config/config.yaml` 文件包含项目的各种配置选项，如时区、存储后端、推送模式等。详细说明请参考文件中的注释。

### 依赖文件

`requirements.txt` 文件列出了项目所需的所有 Python 依赖包及其版本范围。

**Section sources**
- [config/config.yaml](file://config/config.yaml#L1-L187)
- [requirements.txt](file://requirements.txt#L1-L7)